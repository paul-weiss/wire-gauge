[package]
name = "backend-aeron"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
harness = { path = "../harness" }
rusteron-client = { version = "0.2.5", features = ["static"] }
rusteron-media-driver = { version = "0.2.5", features = ["static"] }
