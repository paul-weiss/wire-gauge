[package]
name = "wire-gauge"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
backend-sockets = { path = "../backend-sockets" }
clap = { version = "4", features = ["derive"] }
harness = { path = "../harness" }
